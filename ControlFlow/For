for loop - executes code for a set number of iterations, or until terminate/exit is reached

for loop_count in [reverse] from.. to [by step] loop
  code;
end loop;

step - optional step size, defaulted at 1
reverse - decrement instead of incrementing loop_count

