Temporary Tables only exist as long as session or transaction is live, the moment the session/transaction ends - temporary tables get dropped

CREATE TEMPORARY/TEMP TABLE table(...);

If a regular table and a temporary table, both created with a common name. When retrieving data by the shared name using select, temporary table has higher priority
